// MOVED — this logic now lives in the separate gc-email-agent service
// (gc-email-agent/lib/email-classify.ts), which owns all Gmail access.
// Left here (rather than deleted) only because this environment doesn't
// allow deleting output files once written.
export {};
